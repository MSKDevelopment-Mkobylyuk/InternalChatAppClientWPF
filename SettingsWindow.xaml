<Window x:Class="InternalChatAppClientWPF.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings"
        Width="320"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#121324">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0 0 0 10">
            <TextBlock Text="Server IP Address:" Foreground="White" Margin="0 0 0 5" />
            <TextBox x:Name="IpTextBox" Height="25" Background="#2A2A3D" Foreground="White" BorderThickness="0" Padding="6" />
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0 0 0 10">
            <TextBlock Text="Port:" Foreground="White" Margin="0 0 0 5" />
            <TextBox x:Name="PortTextBox" Height="25" Background="#2A2A3D" Foreground="White" BorderThickness="0" Padding="6" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" >
            <Button Content="Cancel" Width="70" Margin="0 0 10 0" Click="CancelButton_Click" Style="{StaticResource ClickDarkenButtonStyle}"/>
            <Button Content="Save" Width="70" Click="SaveButton_Click" Style="{StaticResource ClickDarkenButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>